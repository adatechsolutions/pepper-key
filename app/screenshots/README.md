# Screenshot gate

Do not replace this file with a mock acceptance artifact.

Before catalog submission, add `ss0.png` captured from the reviewed version 0.1 build. Because PepperKey takes over USB, a generated frame is permitted by the catalog rules, but it must still match the tested on-device screen and use the exact qFlipper format: 512 by 256 pixels, landscape, with only black and RGB 254, 138, 44.
